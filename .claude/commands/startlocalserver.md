Start the local Next.js development server using `npm run dev`.

Run the command and monitor the output until the server is ready. Report the URL where the server is running (typically http://localhost:3000).

If there are any errors during startup, diagnose and report them to the user.
